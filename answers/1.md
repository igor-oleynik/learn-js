##Задача 20
###
```js
const a = null;
const b = '';
const c = 'test';
const d = undefined;

let massage 

console.log(massage);
```

##Задача 21
###
```js
const a = null;
const b = '';
const c = 'test';
const d = undefined;

let massage = a === "" ? "one" : null
    b === "" ? "null" : null
        c === "" ? "three" :
            d === "" ? "four" : null

console.log(massage);
```
##Задача 22
###
```js
let transport = 'machine';

transport = 'car' > 'car' ? 'car' :
    'motorcycle' > 'motorcycle' ? 'motorcycle' :
        'bicycle' > 'motorcycle' ? 'motorcycle' : 'something other'

console.log(transport);
```
